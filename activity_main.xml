<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">

    <!-- Mortgage Principal Amount Input -->
    <EditText
        android:id="@+id/et_principal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="48dp"
        android:padding="12dp"
        android:hint="Enter Mortgage Principal Amount"
        android:inputType="numberDecimal"/>

    <!-- Interest Rate Input -->
    <EditText
        android:id="@+id/et_interest_rate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="48dp"
        android:padding="12dp"
        android:hint="Enter Interest Rate (%)"
        android:inputType="numberDecimal"/>

    <!-- Amortization Period Input -->
    <EditText
        android:id="@+id/et_amortization_period"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="48dp"
        android:padding="12dp"
        android:hint="Enter Amortization Period (years)"
        android:inputType="number"/>

    <Button
        android:id="@+id/btn_calculate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:minHeight="48dp"
        android:minWidth="48dp"
        android:text="Calculate" />


    <!-- Monthly Payment Display -->
    <TextView
        android:id="@+id/tv_result"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Monthly Payment: "
        android:textSize="18sp"
        android:layout_marginTop="24dp"/>

</LinearLayout>
